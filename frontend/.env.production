# Production Environment Configuration
# Uses relative URL so backend is on same domain/subdomain
# Example: if frontend is on https://app.yourdomain.com
# then backend API will be accessed at https://app.yourdomain.com/api
REACT_APP_API_BASE_URL=
NODE_ENV=production

